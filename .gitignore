result
result-*
.direnv

# Written into the source tree by the builder: logos-view-generator emits the
# interface and glue here in preConfigure, so it is output, not source.
generated_code/

rust-core/target/
build/

# What the walkthrough captures. The tutorial's figures are served from the
# published report, laid down there by CI from the run that took them, so a
# copy in the tree would only ever be a stale second one.
doctests/images/
